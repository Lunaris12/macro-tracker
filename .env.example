# Get a free API key at https://fdc.nal.usda.gov/api-key-signup
# Without a key the app falls back to DEMO_KEY, which has very low rate limits
# (about 30 requests/hour) and will start failing quickly.
USDA_API_KEY=DEMO_KEY
PORT=3000
